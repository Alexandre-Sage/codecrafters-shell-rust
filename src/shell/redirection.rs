use super::input_parser::redirection_context::RedirectionContext;

pub struct RedirectionManager;

impl RedirectionManager {
    pub fn write_to_file(&self, context: RedirectionContext) {
        todo!()
    }
}
